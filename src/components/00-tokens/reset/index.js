import { css } from "styled-components";

//Scorched Earth! The approch I have found to be the most reliable

const Reset = css`
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}
`;

export default Reset;
